<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NoticeBoard" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <!-- Admin Dashboard theme -->
    <style name="AdminTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Define the primary and accent colors -->
        <item name="colorPrimary">@color/gold</item>
        <item name="colorPrimaryDark">@color/gold</item>
        <item name="colorAccent">@color/gold</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Define the button color -->
        <item name="buttonTint">@color/gold</item>

        <!-- Add other customizations as needed -->
    </style>

    <!-- Officials Dashboard theme -->
    <style name="OfficialsTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Define the primary and accent colors -->
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/gray</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Define the button color -->
        <item name="buttonTint">@color/gray</item>

        <!-- Add other customizations as needed -->
    </style>

</resources>

